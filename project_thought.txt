一些比较小的能写的点:
1. 用二进制的词典加速:https://github.com/raulmur/ORB_SLAM2/pull/21
2.　回环检测　去除动态物体后的回环
3. 回环　在词典中去除影子部分的特征点，防止一天当中不同时刻影子不同造成的回环难以检测问题．但没有一天不同时刻的数据集可用．不能做实验

实验部分：
- 轨迹采用十次取平均的方式，不同次数差异较大


实例分割的点：
1.　识别行人而不是所有的人，防止汽车座位上的人被识别

用实例分割结合师姐的三角网格／光流，因为三角网格／光流会出现动态判断错误的情况，那么这个时候就可以用实例分割辅助，当上一帧还是动态，下一帧就没有识别动态，但是仍然分割出了这个物体，那么就继续跟踪分割三帧，在三帧内物体都是识别静态，那么就判定该物体静态，停止跟踪．
三角网格／光流辅助实例分割失败，当实例分割失败，但已经判定过该物体仍有动态点存在，那么仍然去除该动态点
稠密光流／三角网格辅助先验静态物体分割

相机位姿的计算，（1）静态部分点　（2）ransac得到的内点　　对（１）和（２）的数量进行比较，选择位姿计算的特征点集

### 整体思路一:
- 目标跟踪(不进行动态判定): 实例分割 + camshift跟踪,辅助实例分割结果. 还是用权值保留可能动态物体点,这里就在第一部分实例分割这里多做实验, 写保证目标分割和识别的准确
- 利用光流或其他 判断是否为动态物体, 做不同光流的实验????? 或者结合双目/rgbd的特点,使用不同的方法判定是否为动态, 做鲁棒性位姿实验
- 利用实例分割和camshift跟踪,建立去除非绝对静态点的词典, 回环判断

### 整体思路二:
- 

